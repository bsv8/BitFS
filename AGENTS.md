- 中文和我交流，文档，注释都是中文，代码里错误信息是英文
- 随时记住我们是要做一个基于 MultisigPool 费用池的付费文件传输系统，系统里的费用池，支付都是真实的链上支付，不是数据库的金额记录
- 我提出的需求也有不合适，有更好方案的时候，你可以提出建议，协商一致后再行动
- 不要把过时的代码用分支兼容性保留下来，这样只会让代码臃肿，不停的误导后来设计去建立在过时的地基上
- 使用 /home/david/.gvm/gos/go1.26.0/bin/go 工具链
- 我倾向于代码即文档,所以写代码的人应该看代码(文档可能和代码会脱节)
- 文档目前只用来和人类交流,比如需求和报告等.人类希望看到的文档少而精确通俗易懂,不用太多的阐述和例子,例子很重要但是不要太多(不要枚举)
- 当需求被修正后,之前错误理解需求产生的代码要清除干净,留在哪里虽然不用但是会影响以后的思路
- 我们执行代码即文档，所以在代码里应该有简短中文设计描述，不用单独生成文档，没人看，还要维护文档同步
